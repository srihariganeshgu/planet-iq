# 🌍 PlanetIQ

**PlanetIQ** is an AI-powered educational quiz game focused on environmental awareness and sustainability. Players choose a hero, select difficulty levels, and answer topic-based questions while receiving real-time AI-generated hints powered by Google Gemini. Progress is tracked via Firebase and players compete on a live leaderboard.

---

## ✨ Features

- 🎮 **Interactive Quiz Gameplay** — Multi-level quiz with lives, streaks, and scoring
- 🤖 **AI-Powered Hints** — Context-aware hints generated by Google Gemini (weak / medium / strong)
- 🦸 **Hero Selection** — Choose your character before entering the game
- 📊 **Leaderboard** — Real-time rankings stored in Firebase Firestore
- 🔐 **Authentication** — Firebase Auth with protected routes
- 📚 **Learn Mode** — Educational content to reinforce quiz topics
- 🎨 **Animated UI** — Smooth transitions with Framer Motion
- 🌙 **Dark Mode** — Theme toggle via `next-themes`

---

## 🛠️ Tech Stack

| Category         | Technology                          |
| ---------------- | ----------------------------------- |
| Framework        | React 18 + TypeScript               |
| Build Tool       | Vite                                |
| Styling          | Tailwind CSS + shadcn/ui (Radix UI) |
| Animations       | Framer Motion, Lottie React         |
| State Management | Zustand                             |
| Data Fetching    | TanStack React Query                |
| Forms            | React Hook Form + Zod               |
| Routing          | React Router DOM v6                 |
| AI Integration   | Google Generative AI (Gemini)       |
| Backend / DB     | Firebase (Auth + Firestore)         |
| Charts           | Recharts                            |

---

## 🚀 Getting Started

### Prerequisites

- [Node.js](https://nodejs.org/) v18 or higher
- [npm](https://www.npmjs.com/) or [bun](https://bun.sh/)
- A [Firebase](https://firebase.google.com/) project
- A [Google Gemini API key](https://aistudio.google.com/app/apikey)

### Installation

```bash
# 1. Clone the repository
git clone https://github.com/your-username/planetiq.git
cd planetiq

# 2. Install dependencies
npm install
# or
bun install

# 3. Set up environment variables
cp .env.example .env
# Fill in your API keys in .env (see below)

# 4. Start the development server
npm run dev
```

The app will be available at `http://localhost:5173`.

---

## 🔑 Environment Variables

Create a `.env` file in the root directory with the following variables:

```env
VITE_GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
VITE_FIREBASE_API_KEY=YOUR_FIREBASE_API_KEY_HERE
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=000000000000
VITE_FIREBASE_APP_ID=1:000000000000:web:0000000000000000000000
```

> ⚠️ Never commit your real API keys. The `.env` file is listed in `.gitignore`.

---

## 📁 Project Structure

```
planetiq/
├── public/                   # Static assets
├── src/
│   ├── components/           # Reusable UI components
│   │   ├── game/             # Game-specific components
│   │   └── ui/               # shadcn/ui component library
│   ├── data/                 # Static quiz question data
│   ├── hooks/                # Custom React hooks
│   ├── lib/
│   │   ├── firebase.ts       # Firebase initialization
│   │   └── utils.ts          # Utility helpers
│   ├── pages/                # Route-level page components
│   │   ├── HomePage.tsx
│   │   ├── HeroSelection.tsx
│   │   ├── LevelSelection.tsx
│   │   ├── GamePlay.tsx
│   │   ├── Leaderboard.tsx
│   │   ├── Learn.tsx
│   │   └── NotFound.tsx
│   ├── services/
│   │   ├── geminiService.ts  # Google Gemini AI integration
│   │   ├── authService.ts    # Firebase Auth helpers
│   │   └── userService.ts    # Firestore user operations
│   ├── store/                # Zustand state stores
│   ├── types/                # TypeScript type definitions
│   ├── App.tsx               # Root component with routing
│   └── main.tsx              # App entry point
├── .env                      # Environment variables (gitignored)
├── .env.example              # Example env template
├── index.html
├── tailwind.config.ts
├── vite.config.ts
└── package.json
```

---

## 📜 Available Scripts

| Script            | Description                      |
| ----------------- | -------------------------------- |
| `npm run dev`     | Start development server         |
| `npm run build`   | Build for production             |
| `npm run preview` | Preview production build locally |
| `npm run lint`    | Run ESLint                       |

---

## 🚢 Deployment

This project is configured for **Firebase Hosting**.

```bash
# Build the app
npm run build

# Deploy to Firebase
firebase deploy
```

Make sure your `firebase.json` and `.firebaserc` are configured correctly before deploying.

---

## 📄 License

This project was built as a **Capstone Project**. All rights reserved.
